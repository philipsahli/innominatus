apiVersion: score.dev/v1b1
metadata:
  name: test-graph-app
containers:
  main:
    image: nginx:latest
    ports:
      - name: http
        port: 80
        protocol: TCP
resources:
  db:
    type: postgres
    properties:
      size: small
environment:
  type: kubernetes
  ttl: 1h
