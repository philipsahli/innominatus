admin:
  defaultCostCenter: "engineering"
  defaultRuntime: "kubernetes"
  splunkIndex: "orchestrator-logs"

# Provider sources (infrastructure and service providers)
# Providers can be loaded from filesystem or Git repositories
providers:
  # Filesystem provider (built-in provisioners)
  # Disabled - no workflows defined yet
  - name: builtin
    type: filesystem
    path: ./providers/builtin
    enabled: false

  # Filesystem provider (storage team demo)
  - name: storage-team
    type: filesystem
    path: ./providers/storage-team
    enabled: true

  # Git-based provider examples (commented out - uncomment to use)
  # Storage team provider from Gitea (recommended for production)
  # - name: storage-team
  #   type: git
  #   category: infrastructure
  #   repository: https://gitea.localtest.me/platform-team/storage-provider
  #   ref: v1.2.0  # Pin to stable version tag
  #   enabled: true
  #
  # Storage team dev branch (for testing latest changes)
  # - name: storage-team-dev
  #   type: git
  #   category: infrastructure
  #   repository: https://gitea.localtest.me/platform-team/storage-provider
  #   ref: main  # Track main branch
  #   enabled: false
  #
  # Example service provider (ecommerce team)
  # - name: ecommerce
  #   type: git
  #   category: service
  #   repository: https://github.com/yourorg/ecommerce-provider
  #   ref: v1.2.3  # Git tag
  #   enabled: true

resourceDefinitions:
  postgres: "managed-postgres-cluster"
  redis: "redis-cluster"
  volume: "persistent-volume-claim"
  route: "ingress-route"
  vault-space: "vault-application-namespace"

policies:
  enforceBackups: true
  allowedEnvironments:
    - "development"
    - "staging"
    - "production"

workflowPolicies:
  # Root workflows directory
  workflowsRoot: "./workflows"

  # Required platform workflows that must run for all deployments
  requiredPlatformWorkflows:
    - security-scan
    - cost-monitoring

  # Product workflows that are allowed to run
  allowedProductWorkflows:
    - ecommerce/database-setup
    - ecommerce/payment-integration
    - analytics/data-pipeline

  # Override policies - who can override what
  workflowOverrides:
    platform: true   # Platform workflows can override product workflows
    product: true    # Product workflows can override application workflows

  # Execution constraints
  maxWorkflowDuration: "30m"
  maxConcurrentWorkflows: 10
  maxStepsPerWorkflow: 50

  # Security policies
  security:
    requireApproval:
      - production
    allowedExecutors:
      - platform-team
      - infrastructure-teams
    secretsAccess:
      vault: "read-only"
      kubernetes: "namespace-scoped"

  # Allowed step types for security
  allowedStepTypes:
    - terraform
    - kubernetes
    - ansible
    - database-migration
    - vault-setup
    - monitoring
    - validation
    - security
    - policy
    - tagging
    - cost-analysis
    - resource-provisioning

gitea:
  url: "http://gitea.localtest.me"
  internalURL: "http://gitea-http.gitea.svc.cluster.local:3000"
  username: "giteaadmin"
  password: "admin123"
  orgName: "platform-team"

argocd:
  url: "http://argocd.localtest.me"
  username: "admin"
  password: "admin123"

vault:
  url: "http://vault.localtest.me"
  token: "root"
  namespace: ""
