# Example .env file for SQLite development
# Copy this file to .env and customize as needed
# Usage: export $(cat .env | xargs) && ./innominatus

# Database Configuration - SQLite
DB_DRIVER=sqlite
DB_PATH=./data/innominatus.db

# Use in-memory database (fastest, no persistence)
# DB_PATH=:memory:

# Server Configuration
PORT=8081

# Logging
LOG_LEVEL=info

# OIDC (Optional - leave commented for file-based auth)
# OIDC_ENABLED=false
# OIDC_ISSUER=https://keycloak.company.com/realms/dev
# OIDC_CLIENT_ID=innominatus
# OIDC_CLIENT_SECRET=your-secret

# OpenTelemetry Tracing (Optional)
# OTEL_ENABLED=false
# OTEL_ENDPOINT=http://localhost:4318

# AI Features (Optional)
# AI_PROVIDER=openai
# OPENAI_API_KEY=your-key
