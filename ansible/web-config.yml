---
- name: Web Application Configuration Playbook
  hosts: localhost
  gather_facts: no
  tasks:
    - name: Create web configuration marker
      local_action:
        module: copy
        content: "Web application configured successfully by Ansible!"
        dest: ./ansible/web_configured.txt
        
    - name: Display web setup completion
      debug:
        msg: "Web application configuration completed successfully"